{
  "name": "takekazuomi-zenn",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preview": "zenn preview",
    "svg:generate": "node scripts/extract-ascii-to-svg.js",
    "svg:watch": "nodemon --watch articles --ext md --exec 'npm run svg:generate'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "zenn-cli": "^0.4.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
